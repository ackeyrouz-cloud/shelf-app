# Copy this file to .env for local development.
# In production (Render/Railway/Fly.io), set this as an environment variable
# in the platform's dashboard instead — never commit a real key to git.

ANTHROPIC_API_KEY=sk-ant-your-key-here
PORT=3000
